{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the local engineering shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
