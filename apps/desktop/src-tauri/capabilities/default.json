{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Ion's main organizer window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
